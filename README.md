# HOLA es mi primer README.

``` 
 --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     La bandera -b (numero) sera el numero a multiplicar
                                                            [número] [requerido]
  -l, --listar   La bandera -l es para ver si se ven los datos impesosen la
                 consola                 [booleano] [requerido] [defecto: false]
  -m, --max      Limitar el numero por el cual sera multiplicada la base, ej:
                 base * max                   [número] [requerido] [defecto: 10]

```